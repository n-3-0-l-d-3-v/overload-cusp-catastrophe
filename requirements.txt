# Runtime dependencies for the analysis pipeline.
#   pip install -r requirements.txt
# Versions are lower bounds, matching what the reported results were produced
# with. Nothing here is pinned exactly, because nothing in the pipeline depends
# on behaviour that changed within these ranges.

numpy>=1.24
scipy>=1.10
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
statsmodels>=0.14
hmmlearn>=0.3
joblib>=1.3

# Tests, page-count checking and figure inspection.
pytest>=7.4
pypdf>=4.0
pymupdf>=1.24
